# Senior Care Platform («ЗаботаРядом»)
> Веб-платформа взаимной помощи пожилым людям и волонтёрской поддержки с элементами ИИ и адаптивным интерфейсом.

## Ветка: `stage-3-backend-api` (Уровень 3: Ядро бизнес-логики и REST API)

На данном этапе реализована вся серверная логика и RESTful API:
* **`app/Core/Router.php`** — роутер с поддержкой динамических параметров `{id}`, обработкой CORS и JSON-ответов.
* **Сервисы бизнес-логики (`app/Services/`)**:
  * **`GeoMatchingService`** — математическая реализация формулы гаверсинусов (Haversine) для вычисления расстояния между координатами и алгоритм ранжирования подходящих волонтёров по весам (расстояние, рейтинг, баллы опыта).
  * **`SecurityService`** — генератор добрых и понятных секретных проверочных кодов («Василёк», «Ромашка», «Берёза») для защиты от мошенников при визите.
  * **`AntiFraudService`** — интеллектуальный анализатор подозрительных сообщений и фраз (выявление угроз: звонки лже-банков, требования CVV, кодов из СМС).
* **Контроллеры REST API (`app/Controllers/`)**:
  * `AuthController` — регистрация и вход подопечных и волонтёров;
  * `HelpRequestController` — создание заявок, сортировка по дистанции, принятие и завершение визита;
  * `AntiFraudController` — экспресс-проверка подозрительных звонков и выдача рекомендаций;
  * `EducationController` — выдача памяток и уроков;
  * `ChatController` — обмен сообщениями внутри активной заявки.
* **`public/api.php`** — единая входная точка API с автозагрузчиком классов PSR-4.

### Проверка работы API:
```bash
# Запуск встроенного веб-сервера:
php -S localhost:8000 -t public
# Проверка эндпоинта активных заявок:
curl http://localhost:8000/api/requests
```
