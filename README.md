# Senior Care Platform («ЗаботаРядом»)
> Веб-платформа взаимной помощи пожилым людям и волонтёрской поддержки с элементами ИИ и адаптивным интерфейсом.

## Ветка: `stage-4-frontend-accessibility` (Уровень 4: Пользовательский интерфейс и доступность)

На данном этапе разработан полноценный клиентский интерфейс, строго отвечающий требованиям доступности **WCAG 2.1 AAA**:
* **`public/css/accessibility.css`** — специализированные стили для пожилых людей (крупный контрастный режим, увеличенные сенсорные зоны от 64px, чёткие шрифты).
* **`public/js/speech.js` (Web Speech API)**:
  * **Голосовой ввод (STT):** распознавание русской речи для надиктовывания заявок через микрофон.
  * **Синтез речи (TTS):** голосовая озвучка содержимого страниц и обучающих памяток для слабовидящих людей.
* **`public/index.php`**:
  * Переключатель режимов: «Стандартный» / «Для старшего поколения» (с сохранением состояния в `localStorage`).
  * Кнопка быстрой озвучки страницы.
  * Крупные интерактивные плитки категорий помощи («Продукты», «Лекарства», «Бытовая помощь», «ЖКХ/Телефон»).
  * Генерация защитного карточки-баннера с секретным кодовым словом визита.
* **`public/js/app.js`**:
  * Взаимодействие с REST API сервера.
  * Личный кабинет волонтёра: отображение списка заявок с реальным расчётом дистанции в метрах и взятием в работу в один клик.
  * Раздел «Школа безопасности» с озвучкой памяток.

### Проверка интерфейса:
Запустите сервер:
```bash
php -S localhost:8000 -t public
```
Откройте в браузере `http://localhost:8000` и протестируйте голосовую диктовку и озвучку.
