# Senior Care Platform («ЗаботаРядом»)
> Веб-платформа взаимной помощи пожилым людям и волонтёрской поддержки с элементами ИИ и адаптивным интерфейсом.

## Ветка: `stage-2-database-models` (Уровень 2: Слой данных и модели)

На данном этапе реализована серверная модель данных и абстракция СУБД:
* **`app/Core/Database.php`** — синглтон подключения к базе данных через PHP Data Objects (PDO) с нативной поддержкой SQLite (без необходимости разворачивать сервер БД) и MySQL/PostgreSQL.
* **`app/Core/Model.php`** — абстрактная базовая модель с реализацией CRUD-операций (`all`, `find`, `where`, `create`, `update`, `delete`, `query`).
* **Модели предметной области (`app/Models/`)**:
  * `User` — пользователи системы и авторизационные сущности;
  * `Role` — системные роли (подопечный, волонтёр, координатор, администратор);
  * `ElderlyProfile` — адреса, гео-координаты и потребности маломобильных граждан;
  * `VolunteerProfile` — верификация, рейтинг, радиус выезда и навыки волонтёров;
  * `Category` — классификатор направлений помощи;
  * `HelpRequest` — заявки с геопривязкой, статусами и защитными кодами;
  * `ChatMessage` — сообщения коммуникации;
  * `EducationalMaterial` — библиотека памяток и уроков цифровой гигиены;
  * `FraudAlert` — база оперативных предупреждений о мошенниках;
  * `Achievement` — геймификация и бейджи волонтёров.
* **Скрипты базы данных**:
  * `database/migrate.php` — автоматическое развёртывание таблиц из `schema.sql`;
  * `database/seed.php` — наполнение БД демонстрационными данными (4 роли, категории, заявки, памятки).
