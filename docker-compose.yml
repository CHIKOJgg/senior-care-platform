version: '3.8'

services:
  app:
    build: .
    container_name: senior_care_platform
    ports:
      - "8000:8000"
    volumes:
      - .:/var/www/html
    environment:
      - DB_CONNECTION=sqlite
      - DB_DATABASE=database/database.sqlite
      - APP_ENV=local
      - APP_DEBUG=true
